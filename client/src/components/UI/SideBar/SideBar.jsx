import SideBarButton from '../SideBarButton/SideBarButton'

const SideBar = () => {
	return (
		<div>
			<SideBarButton>Сообщество</SideBarButton>
			<SideBarButton>Новости</SideBarButton>
			<SideBarButton>Обновления</SideBarButton>
		</div>
	)
}

export default SideBar
